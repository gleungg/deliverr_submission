# deliverr_submission

Code written in Python and tested with unittest. To run test, simply download both files into the same folder, path into the folder in the command line and run:
python -m unittest
